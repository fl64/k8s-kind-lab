apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: cnp-test
spec:
  endpointSelector: {}
  ingress:
  - fromEntities:
    - all
    toPorts:
    - ports:
      - port: "8000"
        protocol: TCP
      - port: "1234"
        protocol: TCP
      - port: "8443"
        protocol: TCP
      rules:
        http:
        - {}
    #     - method: GET
    #       path: /zhopa
    # # - ports:
